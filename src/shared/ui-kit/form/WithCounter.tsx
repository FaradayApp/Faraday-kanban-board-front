import { FloatingInput, FloatingTextArea, withSymbolsCounter } from '.';

export const InputWithCounter = withSymbolsCounter(FloatingInput);
export const TextAreaWithCounter = withSymbolsCounter(FloatingTextArea);
